;; ex 2.22

;; This produces it in the reverse order because the last item squared is being added to
;; the beginning of the list

;; The second procedure still doesn't work because it combines a list with the squared value
;; which makes the combination a 2 element list where the first element is another list. This is
;; not what we are looking for 
